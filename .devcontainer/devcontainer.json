{
  "name": "Python 3 with Streamlit",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "app.py"
      ]
    },
    "vscode": {
      "settings": {
        "python.pythonPath": "/usr/local/bin/python3"
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter",
        "ms-vscode.remote-containers"
      ]
    }
  },
  "updateContentCommand": "echo '🔄 Updating system and installing dependencies'; \
  [ -f packages.txt ] && echo '📦 Installing system packages' && sudo apt-get update && sudo apt-get upgrade -y && xargs -a packages.txt sudo apt-get install -y || echo 'No system packages to install'; \
  [ -f requirements.txt ] && echo '🐍 Installing Python dependencies' && pip install --user -r requirements.txt || echo 'No Python requirements to install'; \
  pip install --user streamlit; \
  echo '✅ Dependencies installed.'",
  "postAttachCommand": {
    "server": "echo '🚀 Starting Streamlit...' && \
    streamlit run app.py --server.enableCORS false --server.enableXsrfProtection false"
  },
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ],
  "containerEnv": {
    "STREAMLIT_SERVER_PORT": "8501",
    "STREAMLIT_BROWSER_GATHER_USAGE_STATS": "false"
  }
}